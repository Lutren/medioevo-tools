# ASSUMPTIONS

## 2026-05-22 - Fragmentos Cover Asset Gate

- Building the validator and manifest is safe local closure because it creates
  no real cover asset and triggers no external side effect.
- `REVIEW_ASSET_MISSING` is the correct current truth for Fragmentos because no
  asset path, source/license decision or platform dimensions have been chosen.
- A clean technical image check is only a prerequisite for human visual review,
  not publication approval.

## 2026-05-22 - Human Gate Packet + Fragmentos Cover Brief

- Preparing review packets and cover briefs is safe local closure because it
  creates no public asset and triggers no external side effect.
- Fragmentos is the better first visual candidate because its public-safe
  direction can focus on archive, memory and pieces rather than technical
  calibration language.
- Any actual image generation or asset selection should wait for an explicit
  asset production gate and provenance/licensing review.

## 2026-05-22 - Editorial Word/PDF Full-Page QA

- Word COM export is acceptable local evidence because it uses the installed
  Word renderer over the exact private DOCX files and does not call cloud or
  external services.
- Blank-page and edge-contact metrics are sufficient to close automated layout
  smoke, while human editorial taste/print/store QA remains a separate review
  gate.
- Contact sheets are useful review aids, not public artifacts.

## 2026-05-22 - Editorial DOCX Split QA

- A split QA smoke is useful progress because full `artifact-tool` rendering
  of these long manuscripts did not finish in controlled windows.
- The integrated PDFs are relevant companion evidence because they were
  packaged from the same canon10 integrated exports, but they do not replace a
  complete DOCX page-by-page visual review.
- Future closure should either implement page-range DOCX rendering or use a
  manual Word/LibreOffice pass.

## 2026-05-22 - Editorial Internal Exports

- The "export interno equivalente" pending item refers to local private review
  packages like the existing Deriva export, not public publication packages.
- The integrated `14/15/16/17/18_*` canon10 artifacts are the correct source
  set because they already represent the current integrated book outputs.
- Creating both Fragmentos and Calibracion exports is safe local closure
  because the action is reversible, path-scoped, internal and hash-verified.

## 2026-05-22 - OSIT-HYBRID Multi-Seed

- A deterministic synthetic harness is the safest executable substitute because
  only reports/CSV/Source Cards were present, not the original simulator code.
- 100 seeds and audit-seed Source Cards for all six scenarios are sufficient to
  close the local pending item, while leaving external validation blocked.
- The next safe step is documentation/status closure, not publication or remote
  compute.

## 2026-05-22 - Smallville-DUAT Local Evidence

- Revalidating the existing `research/geodia-social-observatory` Smallville
  implementation is the correct closure because the required modules, CLIs and
  tests already exist.
- A fresh `qa_artifacts/smallville_duat/SMALLVILLE_DUAT_20260522` package is
  enough evidence for local closure without opening a new runtime surface.
- OSIT-HYBRID multi-seed is the next safe local technical lane after this
  closure.

## 2026-05-22 - OSIT Epistemic Engine API

- A stdlib local HTTP API satisfies the current "API local minima" requirement
  better than introducing Flask or another dependency.
- `/health` and `/classify` are enough to unblock DUAT Lite and future local
  UI/dashboard work.
- Public release, hosted service and stronger scientific claims remain out of
  scope for this continuation.

## 2026-05-22 - DUAT Lite Local Dashboard

- `tools/duat-lite` is the safer path because the documented `claudio/apps`
  target is not present in this workspace root.
- A stdlib HTML/JS dashboard is sufficient for the current panel requirement.
- Desktop and mobile Edge headless screenshots are acceptable visual QA for
  this local-only tool.

## 2026-05-21 - Wabi Observation Claim Adapter + obsai-core Schema

- The correct automatic continuation was a local adapter/schema/test closeout,
  not publication, cloud execution or provider calls.
- The 12-claim fixture is an internal calibration suite for the adapter and
  ClaimClassifier, not a broad truth claim.
- Absence of `lean`, `lake` and `elan` in the host shell is enough to record a
  formal-validation environment block without installing dependencies.

## 2026-05-21 - Pending System Execution Closeout

- The actionable scope is the MEDIOEVO/CLAUDIO canonical pending snapshot, not
  destructive cleanup of the whole host.
- Items requiring next Windows login, admin/firewall review, owner provenance,
  dependency installation or public/cloud action are not safe automatic local
  executions.
- `prompt_started_at` origin is not available inside this Codex context, so the
  honest handoff value is `not_available_in_codex_context`.
- HTTP/API smoke on `127.0.0.1:8787` is sufficient for the non-visual Wabi UI
  closure in this cycle.

## Wabi MCP Gated-Write Design v0.4
- v0.4 is a design and dry-run layer only; it is not an execution release.
- Prepare-only draft creation remains allowed only in local draft store and does not imply approval or queueing.
- Any future real execution requires a separate run with explicit opt-in, TaskSpec, GhostGate, rollback and WitnessLog.
- Localhost verification is sufficient for this run because external exposure is blocked.
- Public-safe publication remains blocked for this run.
# WABI_LOCAL_APPLY_READY_20260519

- The first safe real apply target is the Wabi canonical workspace at `apps/local/wabi-sabi`.
- `wabi_sabi/**`, `tests/**` and `docs/**` are sufficient for v0.1 local programming proof.
- BRAIN_OS UI/server changes remain separate and are validated through the local server test suite.
- Release to GitHub/medioevo.space requires a cleaner release repository or isolated worktree before staging.
- Asset publication needs explicit owner-reviewed public-safe subset, not wholesale adoption.

# WABI_UI_TASKSPEC_MULTI_SMOKE_20260519

- HTML/text snapshot plus Edge headless screenshot is acceptable evidence when the Browser plugin runtime is unavailable.
- Runtime-stripped assets can be used for future internal review, but not for public release until owner provenance review.
- Multi-file TaskSpec support should remain explicit and reviewable; UI should not invent source changes in JavaScript.

# WABI_VIBE_CODING_MODE_20260520

- Vibe Coding means a faster interactive work loop, not bypassing gates.
- The safe default for cloud is still dry-run/proposal-only unless the operator explicitly sets both opt-in flags.
- HTTP/HTML smoke is acceptable for this cycle because no Browser automation tool was exposed to this turn.
