# ASSUMPTIONS ENGINE EXTENSIONS 2026-05-22

- The user confirmation `continua` authorized moving past the Fase 0 checkpoint into local, reversible Fase 1/Fase 2/Fase 3 work.
- Generic `CityState` JSON persistence is sufficient for new `Agent` fields when accompanied by a round-trip test.
- The Godot RPG check should remain validation-only because the RPG lane is private.
- "Física propia por partícula" is satisfied by existing per-body physics plus the added determinism test; no integrator swap to Verlet was assumed.
- "Psicólogo" is a gameplay repair system, not a scientific or medical claim.
- Edge/CDP headless visual QA is acceptable for local regression evidence when no direct in-app browser automation tool is exposed.
