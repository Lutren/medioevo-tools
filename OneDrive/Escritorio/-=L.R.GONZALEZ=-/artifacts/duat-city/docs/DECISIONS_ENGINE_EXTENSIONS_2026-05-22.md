# DECISIONS ENGINE EXTENSIONS 2026-05-22

- One canonical tree: `artifacts/duat-city`; no `duat-city-v2`, no move, no parallel copy.
- Stale FibMob/old unified tree remains historical and marked, not imported into runtime.
- EML canonical implementation is bounded sigmoid, not legacy `exp(x) - log(y)`.
- Theater is a first-class `TileType`/building surface plus procedural profile generator output, not a separate engine.
- Academy installs deterministic role-affinity skill packs through `Agent.skills`.
- Psychologist is a role plus brain repair routine with witnesslog evidence, framed as game engineering.
- Godot RPG validation stays private and headless; no publication action is implied by passing validators.

