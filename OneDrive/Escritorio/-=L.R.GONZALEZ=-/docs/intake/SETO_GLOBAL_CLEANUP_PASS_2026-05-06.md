# SETO Global Cleanup Pass - 2026-05-06

Status: `ROOT_CLEANUP_EXECUTED_SAFE_BOUNDARIES_PRESERVED`

## Scope

This pass cleaned only verifiable local residue under
`C:\Users\L-Tyr\OneDrive\Escritorio\-=L.R.GONZALEZ=-`.

No private game/TCG material, secrets, model weights, envs, releases, product
bundles, vendor/offensive repos, account dashboards, external publication
targets or source-of-truth duplicates were deleted.

## Opening Evidence

- `python tools\release\pending_review.py --write --quiet`:
  `active_dedup=0`, `claudio_open=0`.
- `python tools\release\audit_repo.py`: 12,591 files and 1,991 dirs in the
  root workspace snapshot.
- `python tools\release\find_large_files.py --limit 25 --min-mb 20 --json`:
  the largest active files were Claudio runtime DB/log artifacts, not safe
  deletion targets without a runtime policy.
- `python tools\release\find_duplicates.py --mode hash --limit 20 --json`:
  largest exact duplicate groups were private/TCG/assets/vendor style groups,
  so they stayed in `REVIEW` or blocked lanes.

## Cache Gate Fix

The first dry-run showed the cache cleaner would approve caches inside
`tools\pentest_repos`, `core\sadtalker` and `core\wav2lip`. The tool was
corrected before execution so these boundaries remain excluded.

Changed tool:

- `tools\release\cleanup_regenerable_cache.py`

## Deletions Executed

All deletions were limited to approved regenerable cache directory names under
the workspace root.

| pass | dirs | files | bytes | evidence |
|---|---:|---:|---:|---|
| original dry-run, not executed | 140 | 568 | 6,412,065 | `qa_artifacts\release_validation\seto-cache-cleanup-dry-run-2026-05-06-global.json` |
| corrected dry-run, not executed | 129 | 552 | 6,395,259 | `qa_artifacts\release_validation\seto-cache-cleanup-dry-run-2026-05-06-global-v2.json` |
| corrected execution | 129 | 552 | 6,395,259 | `qa_artifacts\release_validation\seto-cache-cleanup-result-2026-05-06-global.json` |
| post-test residue execution | 11 | 46 | 307,125 | `qa_artifacts\release_validation\seto-cache-cleanup-final-result-2026-05-06-global.json` |
| post-runtime residue execution | 19 | 95 | 1,193,296 | `qa_artifacts\release_validation\seto-cache-cleanup-final-result-2-2026-05-06-global.json` |
| PowerShell containment cleanup after daemons stopped | 31 | 205 | 3,648,880 | resolved paths under workspace root before `Remove-Item` |
| final self-cache cleanup | 1 | 1 | 15,176 | `tools\release\__pycache__` removed and `Test-Path` returned `False` |
| external pytest/window residue cleanup | 64 | 630 | 11,277,147 | repeated Python test process stopped, then resolved paths under workspace root before `Remove-Item` |
| final Wabi validation residue cleanup | 45 | 564 | 10,616,514 | after Wabi job queue/window validation, resolved paths under workspace root before `Remove-Item` |

Total deleted by this pass after corrected execution: 300 cache directories,
2,093 files, 33,453,397 bytes.

## Background Processes

Repeated cache validation showed caches were being regenerated by live Claudio
processes. The cleanup stopped 21 targeted background processes whose command
lines matched Claudio local servers/watchdogs only:

- `run_claudio_always_alert_watchdog.ps1`
- `tools\claudio_always_alert_watchdog.py`
- `pythonw -m http.server 7474 --bind 127.0.0.1`
- `tools/startup_sequence_server.py`
- `claudio_api_server.py`
- Flask `apps.hormiguero_mission_control.app` on ports 5050, 5051 and 5052

Codex/node processes for this session were not stopped.

Additional residue was created by stray `pytest tests/ -x ...` runs and a
scripted `wabi_sabi.cli.main auto` process left from window testing. Those exact
processes were stopped as well. Total targeted process terminations during this
cleanup: 29. Codex/node processes for this session were left running.

## Validation

- `python -B -m pytest -q -p no:cacheprovider` in
  `apps\local\wabi-sabi`: 34 passed.
- `python -B -m compileall -q wabi_sabi tests` in
  `apps\local\wabi-sabi`: passed.
- `.\wabi.cmd auto "ejecuta diagnostico" --json`:
  `route=local_agent`, `gate=APPROVE`.
- `.\wabi.cmd auto "analiza el repo y decide que conviene usar" --dry-run --json`:
  `route=codex_dry_run`.
- `.\wabi.cmd auto "responde exactamente WABI_AUTO_CODEX_ROUTED_OK" --json`:
  `route=codex`, output `WABI_AUTO_CODEX_ROUTED_OK`, sandbox `read-only`,
  approval `never`, session `ephemeral`.
- `.\wabi.cmd auto "responde como Codex exactamente WABI_BACKGROUND_OK" --background-codex --json`:
  `route=codex_background`, job finished `done`, output `WABI_BACKGROUND_OK`.
- `wabi-window.cmd -Here` with `/status` and `/exit`: returned without leaving
  a Python process running after stdin handling was fixed in `wabi-window.ps1`.
- `.\wabi.cmd "crea una funcion que lea un archivo y resuma sus lineas" --apply --target helpers.py`
  against a temp workspace: scoped patch applied and `py_compile_passed`.
- `python -B COMMS\tools\validate_seto_comms.py --json --fail-on-errors`:
  `PASS`, errors `[]`.
- `python -B -m pytest tests -q -p no:cacheprovider`: 87 passed.
- `python -B tools\release\scan_secrets.py --path apps\local\wabi-sabi --json --fail-on-findings`:
  `count_reported=0`.
- `python -B tools\release\scan_secrets.py --path COMMS --json --fail-on-findings`:
  `count_reported=0`.

## Still Blocked

These lanes are intentionally not cleaned by a global sweep:

- Secrets and secret-like paths.
- Private game, TCG, RPG, lore and asset boundaries.
- `E:\` asset trees and Downloads sources without ficha/hash/canonical landing.
- Env folders and package caches needed for installed toolchains.
- Product builds, ZIP/EXE releases and commercial bundles.
- Runtime SQLite DBs and logs without a rotation/retention policy.
- Vendor/offensive repos such as `tools\pentest_repos`.
- Exact duplicates whose canonical copy is still `INFERENCIA` or `REVIEW`.
- Nested dirty repos `-=LIBROS`, `claudio` and `tools\claw-code`; they need
  product-specific intake/commit plans, not broad deletion.

## Next Safe Cleanup Lane

1. Define a Claudio runtime log/DB retention policy before touching
   `runtime/*.db`, `runtime/*.sqlite3` or long logs.
2. Continue SETO duplicate fichas batch 2 for small exact text duplicates.
3. Decide canonical ownership for website/assets duplicates before deletion.
4. Review autostart/task registration separately before re-enabling daemons.
