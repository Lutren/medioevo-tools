# Brain OS Lite

Classification: `OPEN_CANDIDATE`

Current path: `-=MEDIOEVO=-\-=LIBROS\claudio\brain_os`

Purpose: developer tooling subset.

Before release:

- isolate public-safe CLI/docs;
- remove local runtime state;
- scan imports and secrets;
- add install/test commands.
