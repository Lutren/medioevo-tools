# Claudio OS Blueprint

Classification: `OPEN_CANDIDATE`

Current likely canonical path: `PRODUCTOS_MEDIOEVO\claudio_os_blueprint`

Purpose: developer blueprint and build handoff.

Boundary: do not claim ISO/QEMU release until verified.

Before release:

- choose canonical copy;
- compare duplicate blueprint paths;
- scan secrets;
- document WSL dependencies;
- package as blueprint only.
