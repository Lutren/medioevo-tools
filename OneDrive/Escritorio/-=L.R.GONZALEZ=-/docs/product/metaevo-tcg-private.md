# MetaEvo TCG Private

Classification: `PRIVATE`

Current path: `-=MEDIOEVO=-\-=LIBROS\metaevo-tcg`

Rule: do not publish, open-source, or include in free developer packages.

Detected scripts include build/lint/package commands, but they are private-lane only.

Before any future commercial game release:

- create separate release checklist;
- create private manifest;
- review assets and licenses;
- keep source private.
