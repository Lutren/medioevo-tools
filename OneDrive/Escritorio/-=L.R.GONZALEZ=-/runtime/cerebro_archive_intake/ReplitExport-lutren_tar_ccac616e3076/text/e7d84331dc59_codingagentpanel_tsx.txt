import { PixelBorder, ModeBadge } from "./custom/PixelComponents";

export default function CodingAgentPanel() {
  return (
    <div className="space-y-4">
      <div className="font-display text-xs border-b border-border pb-1 font-bold flex justify-between">
        <span>DUAT CODING AGENT // HARNESS</span>
        <ModeBadge mode="REVIEW_REQUIRED" />
      </div>

      <div className="text-center font-mono text-[9px] text-primary border border-primary bg-primary/10 p-1 mb-2">
        [LOCAL-BACKEND-REQUIRED]
      </div>

      <PixelBorder className="space-y-2">
        <div className="text-[10px] font-display font-bold text-accent">CONSERVATIVE WORKFLOW</div>
        <ol className="list-decimal list-inside font-mono text-[10px] text-foreground/80 space-y-1 ml-4">
          <li>INIT — escanear directorio</li>
          <li>SCAN — leer archivos relevantes</li>
          <li>VERIFY — confirmar estado antes de cambio</li>
          <li>TASK — describir tarea mínima</li>
          <li>APPLY — parche mínimo</li>
          <li>REVERIFY — confirmar integridad</li>
          <li>WITNESS — registrar en WitnessLog</li>
        </ol>
      </PixelBorder>

      <div className="space-y-2 font-mono text-[10px]">
        <div className="text-xs font-display font-bold">PROVIDER MODE (CONCEPTUAL)</div>
        <div className="flex flex-col gap-1">
          <label className="flex items-center gap-2"><input type="radio" name="provider" className="accent-primary" defaultChecked /> offline</label>
          <label className="flex items-center gap-2 opacity-50"><input type="radio" name="provider" className="accent-primary" disabled /> openai</label>
          <label className="flex items-center gap-2 opacity-50"><input type="radio" name="provider" className="accent-primary" disabled /> anthropic</label>
          <label className="flex items-center gap-2 opacity-50"><input type="radio" name="provider" className="accent-primary" disabled /> ollama</label>
        </div>
      </div>

      <PixelBorder className="border-destructive bg-destructive/5 space-y-2">
        <div className="text-[10px] font-display font-bold text-destructive">BLOCKED COMMANDS</div>
        <div className="flex flex-wrap gap-1">
          {["rm -rf", "git reset --hard", "DROP TABLE", "sudo rm", "overwrite no-backup"].map(cmd => (
            <span key={cmd} className="bg-destructive text-destructive-foreground px-1 text-[9px] font-mono">{cmd}</span>
          ))}
        </div>
      </PixelBorder>

      <div className="text-[9px] font-mono text-foreground/50 border border-border border-dashed p-1 mt-4 text-center">
        Ejecución real de repo: LOCAL-BACKEND-REQUIRED.<br/>Esta interfaz es PROTOTIPO.
      </div>
    </div>
  );
}